<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/text_gray_13</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_primary</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!--<item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowTranslucentStatus">true</item>-->
        <!-- Customize your theme here. -->
        <item name="android:windowEnableSplitTouch">false</item><!-- disable multiple touch-->
        <item name="android:splitMotionEvents">false</item>

        <!--<item name="android:windowFullscreen">true</item>-->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">never</item>
    </style>

    <style name="Theme.ChatApp" parent="Base.Theme.ChatApp" />

    <!--
TextAppearance.Widget.TabWidget
-->
    <style name="TabTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">12sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerSize50Percent" parent="">
        <item name="cornerSize">7%</item>
    </style>

    <style name="SquareImageView" parent="">
        <item name="cornerSize">0%</item>
    </style>

    <style name="circleImageView" parent="">
        <item name="android:background">@drawable/bg_circle_white</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo</item>
        <item name="windowSplashScreenAnimationDuration">1200</item>
        <item name="postSplashScreenTheme">@style/Theme.ChatApp</item>
    </style>

</resources>